# Table S8.2

**Paper item / 文章条目.** Table S8.2 of the supplementary material.  
补充材料 Table S8.2。

**How to produce it / 生成方法.**

```bash
bash scripts/table_S8_2.sh
# same inputs as Table III; or
python make_tables.py --only Table_S8_2
```

**Inputs / 输入.** Same as `../Table_III/`.  
与 `../Table_III/` 相同。

**Files / 文件.** `table_s8_2.csv`, `table_s8_2_unrounded.csv` (also the channel index of the chosen pair), `provenance.txt`.  
`table_s8_2.csv`、`table_s8_2_unrounded.csv`（另含所选电极对的通道号）、`provenance.txt`。
