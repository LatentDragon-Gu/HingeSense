# Versions used for the results shipped in results/ (Python 3.11, CUDA 13.0 build of PyTorch).
# Install PyTorch from the index that matches your CUDA version, e.g.
#   pip install torch==2.12.0 --index-url https://download.pytorch.org/whl/cu130
torch==2.12.0
numpy==2.4.6
pandas==3.0.3
pyarrow==24.0.0
scikit-learn==1.9.0
scipy==1.17.1
matplotlib==3.11.0
